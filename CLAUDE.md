# プロジェクト概要

[Marp](https://marp.app/) を使用したスライド作成リポジトリ。

# 作業開始ルール

**スライドの編集・作成タスクを行う前に、必ず以下のファイルを読み込むこと。**

1. `docs/SPEC.md` — 仕様を把握してから作業する
2. `docs/TODO.md` — 現在の進捗と残タスクを確認する

これらを読まずに作業を開始してはならない。

# ドキュメント構成

| ファイル       | 役割                                                           |
| -------------- | -------------------------------------------------------------- |
| `docs/PLAN.md` | 要件・アイデアの音声入力による自由記述                         |
| `docs/SPEC.md` | 仕様の整理。AIとの壁打ちによる認識合わせ                       |
| `docs/TODO.md` | タスク管理。コンテキストリセット後も作業を再開できる状態を維持 |

## ワークフロー

1. **PLAN.md** に音声入力でアイデアや要望を書き出す
2. **SPEC.md** に整理する（Claudeと壁打ちして認識齟齬をなくす）
3. **TODO.md** を作成してタスク管理する

# ドキュメント管理ルール

## SPEC.md / TODO.md

- スライドの内容を変更・追加したときは、**SPEC.md の該当箇所を実態に合わせて更新する**
- タスクが完了したときは **TODO.md の該当項目を `[x]` に更新する**
- 新たな作業が発生したときは **TODO.md に項目を追加する**
- ユーザーから明示的な指示がなくても、作業の流れの中で必要と判断したら随時更新する

# スライド作成ナレッジ

スライドの作成・編集時に常に守るべきルール。新たな学びが生まれたらここに追記する。

## 情報整理

- 対策や手法を説明するときは、**レイヤー（誰の責務か）を明示する**と聞き手が理解しやすい
- 関連するトピックは**一つの軸でまとめる**と、くどくならずメッセージが明確になる

## レイアウト・スタイル

- `small-text` などのフォントサイズ縮小クラスは原則使わない。使いたくなったら、**1枚のスライドに含める情報量が多すぎないか先に疑う**
  - 半幅レイアウト（`content-image-right` など）の場合は特に情報が詰まりやすい
- 通常スライド（`title`・`section` 以外）では、**最初の h1 がヘッダーバー**になる（上部に固定表示、下にアクセントカラーのライン）。本文の見出しには通常 h2・h3 を使うが、大きく強調したいときは本文にも h1 を使ってよい
- `content-image` レイアウトでテキストを配置するときは、`>` を使った **blockquote（引用）形式**で記述する
- 目次スライドには `<!-- _class: toc -->` を使用する
- `section` クラスのスライドでは、見出しレベルで役割が決まる
  - **h3**: パートラベル（例: `### Part 1`）— アクセントカラーで表示
  - **h2**: セクションタイトル（例: `## 作ったもの紹介`）
  - **h4**: サブタイトル（例: `#### サーバーレスで作った個人アプリの全体像`）— 薄いグレーで表示
  - 各要素は省略可能。必要な組み合わせだけ使えばよい
- `title` クラスのスライドでは、h1 の**前後に `<br>`** を入れて垂直中央に配置する
- `grid` クラスは n 行 2 列のグリッドレイアウト。`<div class="cell">` 内に見出し + リストや段落を配置する
  - 各セルの左にアクセントラインが入る
  - セル見出しはアクセントカラーで表示される
- `column-layout-2` クラスは 2 カラムレイアウト。`<div class="column">` 内にコンテンツを配置する
  - カラム内では h1 も通常の見出しとして表示される（ヘッダーバーにならない）
- `image` クラスは画像メインのスライド。h1（ヘッダー）+ 画像で、画像が中央に配置される
- ユーティリティクラスはレイアウトクラスと**スペース区切りで組み合わせ**られる
  - `align-center`: 垂直中央配置（例: `column-layout-2 align-center`）
  - `all-text-center`: 全テキスト中央揃え（例: `column-layout-2 all-text-center`）
  - `image-shadow`: 画像にドロップシャドウを付与
- 見出しは **h1〜h3 の範囲で使う**。h4〜h6 は原則使わない
- テーブルは原則使わない。構造化データは `grid` レイアウトやリストで表現する

## 記法

- 区切り文字の使い分け
  - h1 のサブタイトル区切り: `—`（ダッシュ）例: `# 番外編 — AI エージェントの機能の使い分け`
  - その他: `:`（コロン）例: `- **DynamoDB**: 状態管理（既読管理）`
- 同一スライド内で h1〜h6 の見出しブロックが連続する場合、ブロック間に `<br>` を挿入して視認性を確保する
  - `---` で区切られた別スライドへの移行には不要
